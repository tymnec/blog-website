<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Eterea Blogs - <%= pageData.title %></title>

        <style>
           body {
  /* Ensure maximum width at all breakpoints */
  max-width: 100%;

  /* Center the content on most viewports */
  margin-left: auto;
  margin-right: auto;

  /* Apply width constraints for smaller screens */
  @media (max-width: 640px) {
    max-width: 640px;
  }

  @media (min-width: 641px) and (max-width: 768px) {
    max-width: 768px;
  }

  @media (min-width: 769px) and (max-width: 1024px) {
    max-width: 1024px;
  }

  @media (min-width: 1025px) and (max-width: 1280px) {
    max-width: 1280px;
  }

  @media (min-width: 1281px) {
    max-width: 1536px;
  }

  padding: 10px;
  font-family: Arial, "Helvetica Neue", Helvetica, sans-serif;
}

                .footer {
                background-color: #282a36;
                color: #ebdfbd;
                padding: 2rem 0;
                text-align: center;
                border-radius: 20px;
                }

                .footer .container {
                max-width: 1024px;
                margin: 0 auto;
                }

                .footer-content {
                display: flex;
                align-items: center;
                justify-content: space-between;
                }

                .logo {
                font-size: 1.5rem;
                font-weight: bold;
                }

                .footer p {
                margin: 0;
                font-size: 0.8rem;
                }

                .social-links {
                list-style: none;
                display: flex;
                justify-content: space-around;
                padding: 0;
                margin: 0;
                }

                .social-links li {
                margin: 0 0.5rem;
                }

                .social-links a {
                color: #ebdfbd;
                font-size: 1.2rem;
                transition: color 0.2s ease-in-out;
                }

                .social-links a:hover {
                color: #d3b774;
                }

                .navbar {
                background-color: #282a36;
                color: #ebdfbd;
                top: 0;
                left: 0;
                padding: 1rem 1rem;
                z-index: 100;
                border-radius: 20px;
                }

                .navbar .container {
                max-width: 1024px;
                margin: 0 auto;
                display: flex;
                align-items: center;
                justify-content: space-between;
                }

                .navbar .logo {
                font-size: 1.2rem;
                font-weight: bold;
                text-decoration: none;
                }

                .navbar .menu {
                list-style: none;
                display: flex;
                margin: 0;
                padding: 0;
                }

                .navbar .menu li {
                margin: 0 0.5rem;
                }

                .navbar .menu a {
                text-decoration: none;
                color: #ebdfbd;
                font-size: 1rem;
                padding: 0.5rem;
                transition: color 0.2s ease-in-out;
                }

                .navbar .menu a:hover {
                color: #d3b774;
                }

                .navbar .menu a.active {
                color: #859926;
                font-weight: bold;
                background-color: #ebdfbd;
                border-radius: 5px;
                }

                .navbar .container .logo .logo-image {
                height: 3rem;
                border-radius: 10px;
                }

                /* Overall Page Styling */
                .post-container {
                max-width: 800px;
                margin: 20px auto;
                padding: 2rem;
                border-radius: 10px;
                background-color: #ebdfbd;
                box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
                }

                h1 {
                text-align: center;
                margin-bottom: 2rem;
                }


        #pageContent {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fbf1c7; /* Gruvbox Light content background color */
            /* border: 1px solid #928374; Gruvbox Light border color */
            border-radius: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Gruvbox Light box shadow */
        }

        #pageName {
            font-weight: bold;
            font-size: 1.5em;
            margin-bottom: 10px;
            color: #282a36; /* Gruvbox pink for page name */
            text-align: center;
            text-decoration: underline;
            text-decoration-color: #8ec07c;
            text-underline-offset: 5px;
            text-decoration-thickness: 2px;
            cursor: pointer;
        }

       
        #author {
            margin-bottom: 10px;
            font-size: 1.2em;
            font-weight: bold;
            color: #282a36; /* Gruvbox pink for author */
            background-color: #8ec07c;
            padding: 10px;
            border-radius: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: max-content;
            cursor: pointer;
        }

        #date {
            margin-bottom: 10px;
            font-size: 1em;
            color: #282a36; /* Gruvbox green for date */
        }

        #category {
            margin-bottom: 10px;
            font-size: 0.9em;
            color: #8ec07c; /* Gruvbox teal for category */
            font-weight: bold;
            text-decoration: underline;
            cursor: pointer;
        }

        #coverImage {
            max-width: 100%;
            height: auto;
            margin-bottom: 20px;
            border-radius: 20px;
        }

        #content {
            line-height: 1.6;
        }
        </style>
    </head>
    <body>
        <nav class="navbar">
            <div class="container">
                <a href="#" class="logo"><img class="logo-image"
                        src="/api/logo"
                        alt="Eterea Logo" /></a>
                <ul class="menu">
                    <li><a href="/" class="active">Home</a></li>
                    <li><a href="/about">About</a></li>
                    <li><a href="/contact">Contact</a></li>
                    <li><a href="/post">Post</a></li>
                </ul>
            </div>
        </nav>
        <div id="pageContent">
            <div id="pageName"><%= pageData.title %></div>
            <div id="author"><%= pageData.author %></div>
            <div id="date">Date: <%= pageData.date %></div>
            <div id="category">Category: <%= pageData.category %></div>
            <img id="coverImage" src="/api/uploads/<%= pageData.image %>"
                alt="<%= pageData.title %> Cover Image">
            <div id="content"><%= pageData.content %></div>
        </div>
        <!-- Footer -->
        <footer class="footer">
            <div class="container">
                <div class="footer-content">
                    <div class="logo">
                    </div>
                    <p>&copy; Copyright 2024 Eterea Blogs. All rights
                        reserved.</p>
                    <ul class="social-links">
                        <li><a href="#"><i class="fa fa-instagram"></i></a></li>
                        <li><a href="#"><i class="fa fa-twitter"></i></a></li>
                        <li><a href="#"><i class="fa fa-github"></i></a></li>
                    </ul>
                </div>
            </div>
        </footer>
    </body>
</html>
